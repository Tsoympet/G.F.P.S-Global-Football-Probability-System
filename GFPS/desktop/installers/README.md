This directory is reserved for desktop installer bundles produced by CI.

- `desktop-release.yml` copies the platform-specific `.msi`, `.dmg`, and `.AppImage` outputs here before uploading artifacts.
- Do not commit installer binaries; they are generated by the build and release workflows.
